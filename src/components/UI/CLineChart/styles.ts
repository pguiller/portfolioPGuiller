import { SxProps } from '@mui/material';

interface CLineChartStyles {
  wrapper: SxProps;
}

export const cLineChartStyles = (): CLineChartStyles => ({
  wrapper: {
    position: 'relative',
  },
});
